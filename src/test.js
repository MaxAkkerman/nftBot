

let res = [1,2,3]
// [4, 3, 2, 1, 0]


function smaller(arr2) {
  // return arr2.map(it=>it=arr2.filter(fi => {
  //   console.log(it, fi)
  //   if (it > fi) return fi
  // }).length)
  
  
  
}

console.log(smaller(res))